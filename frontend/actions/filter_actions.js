// export const UPDATE_BOUNDS = "UPDATE_BOUNDS";

// export const refreshBounds = bounds => ({
//     type: UPDATE_BOUNDS,
//     bounds
// });

// export const updateBounds = bounds => (
//     APIUtil.updateBounds(bounds).then(bounds => dispatchEvent(refreshBounds(bounds)))
// )